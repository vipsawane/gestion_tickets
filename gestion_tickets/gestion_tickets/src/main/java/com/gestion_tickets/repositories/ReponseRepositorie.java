package com.gestion_tickets.repositories;

import com.gestion_tickets.models.Reponse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReponseRepositorie extends JpaRepository<Reponse,Integer> {
}
